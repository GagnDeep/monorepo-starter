# 0004: shadcn/ui over Mantine / MUI

shadcn ships *copy-paste* components into our repo rather than a dependency. We own the code, can theme via CSS vars, and aren't tied to a library's release cadence. Trade-off: more files in `components/ui/`, but they're easy to read and modify.
