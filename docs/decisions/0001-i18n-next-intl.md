# 0001: next-intl over i18next

next-intl is chosen for its first-class App Router integration: type-safe `useTranslations`, server/client parity via `getTranslations`, built-in middleware for locale prefixes, and `Link`/`redirect` helpers that preserve locale. i18next predates App Router and requires more wiring.

Don't migrate.
