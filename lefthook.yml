pre-commit:
  parallel: true
  commands:
    typecheck:
      run: pnpm typecheck
    lint:
      run: pnpm lint
    check-i18n:
      run: pnpm check:i18n
    format:
      glob: "*.{ts,tsx,js,jsx,json,md}"
      run: pnpm exec prettier --check {staged_files}
