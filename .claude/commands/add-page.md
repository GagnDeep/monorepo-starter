---
description: Scaffold a new locale-aware page
---

Run `pnpm gen:page $ARGUMENTS` and then:
1. Show the user the generated page and translation stubs
2. Remind them to translate the `description` keys in non-en locales
3. Suggest running `pnpm verify`
