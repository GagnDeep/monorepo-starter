---
description: Add a new locale
---

Run `pnpm gen:locale $ARGUMENTS` (expects `<code> <label>`). Then:
1. List the keys that need real translations (everything except `locale.*`)
2. Suggest `pnpm verify` to confirm parity
